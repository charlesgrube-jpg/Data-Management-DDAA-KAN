# Colab/Linux Dependencies (GPU Enabled)

# Core
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.22.0
scipy>=1.10.0
pandas>=1.5.0
pyyaml>=6.0
pydub>=0.25.0
tqdm>=4.65.0

# Synthesis (Linux-only friendly)
edge-tts>=6.1.9
gTTS>=2.5.0
transformers>=4.33.0  # For Bark/SpeechT5
sentencepiece>=0.1.99 # For text tokenization
accelerate>=0.21.0    # For optimization
# RVC Dependencies (Manual list to avoid Fairseq/Hydra conflict)
hydra-core>=1.1
omegaconf>=2.1
faiss-cpu==1.8.0
torchcrepe
ffmpeg-python
av
loguru
praat-parselmouth
pyworld
bitarray          # Fairseq runtime dep
sacrebleu>=1.4.12 # Fairseq runtime dep
numpy<2           # RVC requirement
# TTS>=0.22.0          # Coqui TTS (Causes install failures on Colab Py3.10+)

# Fixes for Colab
ffmpeg-python
nest_asyncio

# Feature Extraction (GPU)
nnAudio>=0.3.2
torchaudio>=2.0.0
