# Colab/Linux Dependencies (GPU Enabled)

# Core
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.22.0
scipy>=1.10.0
pandas>=1.5.0
pyyaml>=6.0
pydub>=0.25.0
tqdm>=4.65.0

# Synthesis (Linux-only friendly)
edge-tts>=6.1.9
gTTS>=2.5.0
rvc-python-butter @ git+https://github.com/MisileLab/rvc-python-butter.git    # Patched for Colab Python 3.12
# TTS>=0.22.0          # Coqui TTS (Causes install failures on Colab Py3.10+)

# Fixes for Colab
ffmpeg-python
nest_asyncio

# Feature Extraction (GPU)
nnAudio>=0.3.2
torchaudio>=2.0.0
